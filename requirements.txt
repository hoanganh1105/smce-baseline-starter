# App / UI
streamlit>=1.32

# Core Data Libraries
pandas
numpy
tqdm
scikit-learn==1.9.0
pillow>=11.0.0
joblib
requests

# PaddleOCR Backend (compatible with Python 3.13)
paddleocr
paddlepaddle>=3.2.2
opencv-contrib-python==4.10.0.84
pyclipper
shapely

# VietOCR dependency workaround for Python 3.13.
# Fixes ModuleNotFoundError: No module named 'pkg_resources'.
setuptools<70.0.0

# PyTorch CPU Backend.
# Use extra-index-url so regular dependencies below still resolve from PyPI.
--extra-index-url https://download.pytorch.org/whl/cpu
torch
torchvision

# VietOCR Ecosystem Dependencies
vietocr
einops==0.2.0
gdown==4.4.0
prefetch-generator==1.0.1
imgaug==0.4.0
albumentations==1.4.2
lmdb>=1.0.0
